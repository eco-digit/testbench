ecodigit.git.storage=${user.home}/.local/ecodigit-integration/git
reporting.error-log-location=${user.home}/.local/ecodigit-integration/log/reporting
spring.servlet.multipart.max-file-size=10MB

minio.endpoint=http://localhost:9002

## Postgres
spring.datasource.postgres.jdbcUrl=jdbc:postgresql://localhost:5442/testbench_db
spring.datasource.postgres.username=ecodigit
spring.datasource.postgres.password=ecodigit2425
spring.datasource.postgres.driver-class-name=org\.postgresql.Driver


## Minio
minio.access-key=minioadmin
minio.secret-key=minioadmin

arthur.service.enabled=false

ecodigit.git.acceptedSchemes=http,https,ssh,file

timescale.enabled=false
spring.flyway.enabled=false
oidc.enabled=false

arthur.service.measurements_finished=http://localhost:5000/api/jobs_finished/recent

# Keycloak
keycloak.login-url="/oauth2/authorization/keycloak"

# API
api.version=v1
api.base-url=/api/${api.version}
frontend.base-url=http://localhost:4200
